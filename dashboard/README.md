# Dashboard

The dashboard tracks total cases, reviews, accepted/edited/rejected diagnoses, AI-human agreement, issue types and severity.

Agreement = Accepted / Total Reviewed × 100.

The interactive Streamlit dashboard is implemented in `app.py`.
